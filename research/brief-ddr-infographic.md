# BRIEF — Document Design Researcher — MAKE `infographic` A REAL FAMILY

Deliverable: draft CSV rows + `research/59-infographic-content.md`.
Ruled by the lead 2026-09-11. Task 01a0903d.

## THE PROBLEM
`infographic` is SHIPPED and it is a SHELL. It has a doctype row, a page format and a render
target, and **no design content at all**. Verified:
- `doctypes.csv` `infographic`: **Constraint Set Keys, Structure Key and Region Key are EMPTY**
- `doc-reasoning` `infographic-scaffold`: **Style Key, Palette Key and Typeface Key are EMPTY**

The png handoff builder now exists and works, so this is fully visible in one command:

    python3 scripts/resolve.py --doctype infographic --json > <path>
    python3 scripts/ddi.py handoff --json <path> --format png

Today that prints `(not present in this resolution)` for font-face, font sizes, sections AND
palette. The pipeline is correct; there is nothing behind it.

**This is the v0.1.0-era "that document type is not in my library" symptom in data form**, and
RESUME.md's v0.3 standing rule says a format is DONE only when it is fully in the library AND
produces a good result when the skill runs. **The lead ruled: AUTHOR THE CONTENT, DO NOT UNSHIP.**

## WHAT TO AUTHOR — five things
1. **A structure** with a Section Order suited to a single-canvas data visual, plus
   `Heading Depth Max`, `TOC Depth`, `Front Matter Numbering`, `Caption Position`,
   `Cross-Ref Style`.
2. **Headings in EN, FR and DE** for every section in that order, with `Is Primary` set.
   You authored the heading rows for Ruling N; same shape.
3. **A type scale for the 1080x1350 social canvas.** px-based reasoning, LARGE body — this is
   read at thumbnail size on a phone, not at reading distance.
4. **A palette** and **a typeface pointer** on `infographic-scaffold`.
5. **The Constraint Set Keys and Region Key** on the `infographic` doctype row, or a stated
   reason either is legitimately empty.

## THINGS ALREADY IN THE LIBRARY — check before authoring, do not duplicate
- **The canvas is already encoded.** `px-infographic-portrait` is 285.75 x 357.19 mm, which is
  exactly 1080 x 1350 px at 96dpi. Do not invent a second source for the dimensions.
- **`screen` is an existing Medium** in `type-scales.csv`, currently with one row
  (`report-screen-screen-body`, 11pt). Decide whether the infographic scale is a new
  `scale_key` under `screen`, and say why. 11pt is a reading-distance size and is almost
  certainly wrong here; justify what you choose.
- **`poster-single-canvas`** is the closest existing structure (`headline;call-to-action`).
  Cross-class section reuse is ALLOWED by the standing rule. Reuse where honest, and say what
  you reused and why an infographic needs more or fewer sections than a poster.

## HARD RULES
- **Author in the research/ DRAFT CSVs and load with `research/load-base.py`.** Drafts are
  provenance and are never modified after loading. **NEVER hand-edit `data/base/*.csv`** — it is
  generated by the loader.
- **GENERIC ROWS ONLY.** Any row whose `Brand Scope` is not `generic` is skipped by the loader
  and must not be authored here.
- **EVERY VALUE IS SOURCED OR TAGGED `CONVENTION`.** Do not borrow an authority you cannot
  grep-verify in this library. Bringhurst is cited here ONLY for measure and leading, never
  pagination or screen sizing. Butterick and DIN 5008 are cited NOWHERE. If a ruling names a
  source that does not cover the case, say so instead of borrowing the name — you did exactly
  this for the page-flow rules and it is the behaviour to keep.
- **No git, ever.** The orchestrator owns git.
- `python` is broken here; use **`python3`**.
- Do not tune a value to make a check pass. See the 2026-09-09 precedent, refusing the green tick.

## ACCEPTANCE — this is the bar, set by the lead
    the png handoff for infographic shows ZERO not-present lines
    for font-face, font sizes, sections and palette

Plus: full pytest green, `validate_data.py data/base` clean.

**Paste the actual before/after handoff output.** A green gate is not the proof — the whole
reason this defect existed through a release is that the gate passed while the output said
nothing. Ask what a check would report if the feature produced NOTHING AT ALL; if it would still
pass, it is not a test.

## DELIVERABLE — `research/59-infographic-content.md`
1. Each of the five items: what you authored, and the source or CONVENTION tag for every value.
2. What you REUSED from existing rows and why that reuse is honest rather than convenient.
3. Your reasoning on the type scale — why those sizes for a 1080x1350 canvas read on a phone.
4. Before/after png handoff output, quoted in full.
5. pytest and gate results.
6. Anything you could not source, stated plainly.

Packaging reviews the loaded result after you. Report to me in a few lines when done:
five items authored yes/no, zero not-present lines yes/no, pytest and gate.
