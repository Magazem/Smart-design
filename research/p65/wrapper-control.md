You cannot run code. Produce the document as HTML or Markdown, in this exact layout:

1. First lines: `Doctype: <document type>` and `Design: <design name>`.
2. A line `=== DOCUMENT ===`, then the document. The first heading is the document title.
3. A line `=== STYLE SHEET ===`, then the exact fonts, hex colours and pt sizes you used.
4. Say which sections you omitted for lack of content.

The user's request follows.
