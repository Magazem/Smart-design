# BRIEF — Coverage — RULING M: the docx handoff block is empty. v0.2 BLOCKER.

Repo root: C:\Users\ysuliman\Documents\Ai plugin
DO NOT touch git.
NOTE: research/brief-coverage-L.md is a DIFFERENT, HELD brief. Ignore it. This is your task.

## What I found, running it myself on production data
`resolve.py --doctype report-long-toc --json` piped into `ddi.py handoff --format docx`:

    page:               (not present in this resolution)
    fonts:              (blank)
    font sizes:         ": pt  ->  None half-points"
    TOC heading levels: (not present in this resolution)
    characterSpacing:   (not present in this resolution)
    palette:            (not present in this resolution)
    page flow:          all five rules, correct

The ONLY section that works is page flow, added an hour ago. The handoff block is HOW A DOCUMENT
GETS BUILT — it is what the docx skill is handed. It has been shipping with no page size, no
fonts and no palette since v0.1.0.

## And the existing fix is broken
Mechanism added `display_columns` BY HAND to data/schema-manifest.json. That file is GENERATED by
research/build-manifest.py. I ran the generator from the skill directory: the key was WIPED. So
page flow would silently stop working at the next regeneration. I restored the file; 151 tests
and gate 419 hold. Do not perpetuate the hand edit.

## Deliverable (ONE) — four parts, in this order

### 1. DIAGNOSE BEFORE YOU CHANGE ANYTHING
For report-long-toc, say PER SECTION why it is empty. The candidates are different and the fix
differs accordingly:
- the table has no `display_columns`, so resolve never surfaces the values
- the resolve path never attaches that table at all
- ddi.py reads a column NAME that does not exist in the table
Do not assume all six sections have the same cause. Write the diagnosis down before touching
code; if the cause is not what I guessed, say so.

For reference, ddi.py reads at least these column names: Trim W mm, Trim H mm, Margin Top mm,
Margin Bottom mm, Margin Inside mm, Margin Outside mm, Heading Family, Body Family, Primary,
Secondary, Accent, Background, Foreground.

### 2. FIX IN THE GENERATOR, NEVER IN THE MANIFEST
Put `display_columns` into research/build-manifest.py for constraints AND for every table ddi.py
reads. Then regenerate — from the skill directory, since the generator writes a relative path —
and commit the regenerated manifest as part of the same change.

### 3. END-TO-END HANDOFF TEST
Run the handoff on TWO real doctypes, report-long-toc and cv-generic, and assert page, fonts,
font sizes and palette are all NON-EMPTY, and numeric where they should be numeric. The point is
that an empty block can never ship silently again. A test that only checks the command exits 0
would have passed all through v0.1.0.

### 4. GENERATOR ROUND-TRIP TEST
Regenerate the manifest into a temp directory and assert it equals the committed one BYTE for
byte. That catches the next hand edit automatically. Mind the line endings: the working copy is
CRLF and git stores LF, so compare like with like and say which you compared.

## Verify — paste actual output
1. The handoff block for report-long-toc, in full, with real values.
2. The same for cv-generic.
3. `python3 -m pytest scripts -q` — baseline 151 passed plus 8 subtests; report your delta.
4. `python3 scripts/validate_data.py data/base` — `OK: validated 14 table(s), 419 row(s)`.
5. `git diff --stat` — say what moved and why each file had to.

Python: C:\Users\ysuliman\AppData\Local\Microsoft\WindowsApps\python3.exe

## Report
Message the orchestrator (01a080c5-2001-78b3-bbbe-afaae15edafa), max 10 lines: the per-section
diagnosis, both handoff blocks, the test names, and the tally.
