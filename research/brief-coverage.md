# BRIEF — Coverage — v0.2 phase B1: load the section model to gate zero

Repo root: C:\Users\ysuliman\Documents\Ai plugin
DO NOT touch git.

## Why
Phase A is done. All fifteen previously empty structure keys have drafted section orders, and
204 heading rows across four inputs verify clean. None of it is wired into the loader yet.

## Deliverable (ONE) — the load, green
Three mechanical steps, then prove it.

### 1. Wire the three new heading inputs into research/load-base.py
The T13 block starts at line 430 and currently reads only "18-ats-headings.csv" via the `read()`
helper at line 177. Extend it to read, IN THIS ORDER:
  18-ats-headings.csv, 39-headings-transactional-draft.csv,
  40-headings-longform-draft.csv, 41-headings-marketing-draft.csv
All four now share the same header: canonical_section,heading_text,language,is_primary,source.

CRITICAL: the `n` counter that builds `<section>-<lang>-<n>` must be SHARED ACROSS ALL FOUR
FILES, not reset per file. I have already simulated this: 204 rows, zero collisions. Reset it
per file and you will get duplicate heading_keys.

The T13 CHANGES string hardcodes "81 rows". Make it compute the real count.

### 2. Merge the section orders into research/36-t10-structures-draft.csv
That draft is the ONLY structures source the loader reads. Fill its `Section Order` column from:
  39-t10-section-orders-transactional.csv, 40-t10-section-orders-longform.csv,
  41-t10-section-orders-marketing.csv
Fifteen rows get a value. cv-experienced and cv-academic already have one — DO NOT touch those
two. Every other column on every row stays byte-identical.

Prefer merging programmatically over hand-editing, and keep the merge script in research/ so it
is reproducible.

### 3. Load and prove it
Run research/load-base.py.

## Verify — paste the actual output of each
1. `python3 scripts/validate_data.py data/base` from skill/document-design-intelligence.
   Must be exit 0. Report the table and ROW COUNT — it was 291; headings grows by 123.
2. `git diff --stat skill/document-design-intelligence/data/` — expect headings.csv and
   structures.csv. If any OTHER table changed, that is a finding: report it, do not tidy it.
3. Every one of the 17 structure rows now has a non-empty Section Order. Print the 17 keys with
   their orders.
4. `python3 -m pytest scripts -q` — baseline 138 passed plus 8 subtests.
5. `python3 scripts/resolve.py --doctype letter-formal` and `--doctype invoice-tabular` — both
   must now show a real section order rather than "no section-order guidance".
6. `python3 scripts/resolve.py --doctype cv-generic` — must be unchanged.
7. Report the md5 of data/schema-manifest.json. It should NOT change; the manifest comes from
   build-manifest.py.
8. data/rationale/headings.md is regenerated by this load. Report how many distinct citations it
   now groups, and confirm no group is "(none given)".

## Out of scope — a separate brief follows
Do NOT rewrite the stale comments or add the reuse-rule paragraph to the generator. That is B2.
The one exception is the "81 rows" CHANGES string in step 1, which you must fix because you are
changing the number it reports.

Python: C:\Users\ysuliman\AppData\Local\Microsoft\WindowsApps\python3.exe

## Report
Message the orchestrator (01a080c5-2001-78b3-bbbe-afaae15edafa), max 10 lines: the gate line
with its row count, the diff stat, the test tally, the two resolve results from step 5, the
manifest md5, and the citation count from step 8.
