# cv-second-coder-r2 — independent second coder, round 2 (header treatment only)

**Role.** Fresh, independent second coder per research/82 §7 round 2 for cv (research/82a-cv.md
step 3). Strict independence maintained: the only files opened were `research/82a-cv.md`
(decision procedure + Addendum A), the header-treatment row of `research/82-design-ranking-
protocol.md` §4 (for context, as permitted), and `research/designs-evidence/cv-items.csv`. No
corpus `.md`, recode file, or agreement file was opened.

**Sample.** ids sorted lexicographically from `cv-items.csv` (n=80 coded items), then
```python
import math, random
random.Random("82a:cv").sample(ids, max(min(10, len(ids)), math.ceil(0.25 * len(ids))))
```
= 20 ids: GH:051, NPM:068, GH:076, NPM:040, GH:019, GH:024, NPM:005, NPM:027, NPM:079, GH:013,
NPM:002, GH:055, NPM:067, GH:042, NPM:048, NPM:039, GH:036, GH:061, GH:041, NPM:021.

**Method.** For each sampled id, the `preview_url` was fetched with `curl` into
`research/designs-evidence/tmp-cv3/` (two PDFs — GH:024, GH:036, GH:061 — were rendered to PNG
page-1 at 2x with PyMuPDF for viewing). Each image was viewed and coded strictly by the
`82a-cv.md` decision procedure (image-hero → band → ruled → split → plain-centered → plain-left,
first match wins), applying Addendum A1–A3. Pixel positions/ratios were estimated visually from
the rendered image (no exact-pixel tool available); any test decided within ~10% of its threshold
is flagged and the estimated measurement is recorded, per the procedure's evidence rule.

## Results (n=20)

| id | header | deciding test | measurements/note |
|---|---|---|---|
| GH:051 | plain-left | elimination (2→6) | Promo banner ignored per A3; real page = the white sheet mockup. Name "大白菜" plain white bg, no fill/rule tied to it. Avatar top-right is an image (never creates split, A-def). Name flush left, far from page centre → plain-left. |
| NPM:068 | ruled | rule test (3) | Browser chrome ignored (A3). Thin full-width gray rule sits directly below the header block (name/title/contact icons), before the photo+summary block and before "Work Experience". Header block = name+title+contacts only (A1: summary excluded). Rule ≈ full card width (≥90% live width), gap to contact row ≈0 lines, no text between rule and header block → ruled. |
| GH:076 | plain-centered | test 5 | Name "San Zhang" and contact line both horizontally centred on the page (visually ≈ page centre, well inside 5%). Rule under "Education" heading is a section-heading underline → excluded (A2). No band/rule/split candidates precede it. |
| NPM:040 | plain-left | elimination via (5) fail, confirmed by (3c) fail | Name "JANE DEVELOPER" left-flush, centre offset ≈17% of page width (well past 5%) → fails plain-centered. A full-width rule exists after the summary paragraph, before "EXPERIENCE," but the summary (excluded from header block per A1) sits *between* the header block (name/title/contacts) and that rule, so ruled-test (c) ("no text lies between it and the header block") fails — order here is header→summary→rule, not header→rule→summary as in the A1 worked exceptions. → plain-left. |
| GH:019 | band | band test (2) | Page opens with a rounded blue-gray pill/banner fill directly behind "求职简历 / PERSONAL RESUME" (treated as the top header text, since no other name-sized heading precedes it — the candidate's actual name "余涵" only appears later as a data field in "基本信息", not as a heading). Fill height ≥ cap height (b, clear pass); fill width ≈58–62% of page width, right at the 60% threshold (c, recorded per the within-10% rule); fill ends ≈10% down the page, well above the 40% cutoff (d, clear pass). Coded band on balance, flagging (c) as borderline. |
| GH:024 | plain-left | elimination (2→6) | Name "李维天" top-left, contact icon row directly below (same side, left). No fill/rule behind or under the header block before the intro paragraph (summary is excluded from header, A1, and no rule sits between contacts and paragraph). Section headings (教育背景, etc.) have their own colored icon + underline styling, irrelevant to header (A2, first section is after paragraph anyway). Name far left of page centre → plain-left. |
| NPM:005 | plain-left | elimination (2→6) | Name "Jane Developer" + subtitle top-left on white; below it the background shifts to light gray starting exactly at the "Contact" heading — which is itself the first section heading in this theme (Contact/About/Profiles/Work are all sections). Since the fill/shift coincides with the first section rather than sitting *between* header and section, ruled/band condition (b) fails. Name centre ≈30% of page width from left, far outside the 5% centred band → plain-left. |
| NPM:027 | plain-centered | test 5 | Name "Thomas Davis" (serif, large) and the contact icon row beneath it are both horizontally centred on the page. The rule under "Education" is the section heading's own underline → excluded (A2). |
| NPM:079 | plain-left | elimination (2→6), band (2d) explicitly fails | Left column is a dark sidebar (photo, contact list, education, skills) running the full page height → band test (d) fails outright (full-height fill never "returns to background" above 40% height); it also isn't behind the name's glyphs at all (name sits in the white right column), so (a) fails too. Name "RICHARD HENDRIKS" is centred only within the white right column, not the full page — measured centre offset from true page centre ≈13%, over the 5% threshold → fails plain-centered. No rule spans ≥80%/90% of width (sidebar breaks continuity) → not ruled. → plain-left. |
| GH:013 | ruled | rule test (3) | Small circular avatar (≈8% of page height) is far under the 30%-page-area image-hero bar → not image-hero. A thin double rule sits directly below the header block (name, title, social-icon row — all header per A1) and directly above the summary paragraph: full page width, ~0 line gap, no text intervening → matches the A1 exception pattern exactly (header→rule→summary) → ruled. (A second, similar rule sits between name and title itself; it fails (b) since it's mid-header, not counted.) |
| NPM:002 | plain-left | elimination (2→6) | Two-column CV: name+photo+contacts all confined to a narrow left sidebar card (~25% of page width), page content to the right. No fill distinguishable behind the name (plain white). The only rule near the header spans just the sidebar column width, well under 80%/90% thresholds → sidebar-only rule fails (a), not ruled. Name centred only within its narrow column, not the page (offset ≈74% of a full-width centring, i.e. nowhere near page centre) → plain-left. |
| GH:055 | split | split test (4) | Browser chrome ignored (A3). "YOUR NAME" flush to the left margin; a 4-line contact block (email/phone/website/github) flush to the right margin. Bounding boxes don't overlap horizontally, and they overlap vertically (contact block starts at/above the name's baseline) → all of split (a)-(c) satisfied → split. |
| NPM:067 | image-hero | test 1 | Preview screenshot is a full-bleed starry/nebula photographic background covering the entire visible page (well over 30% of page area) behind "I'm Richard Hendriks." → image-hero (priority test, no further tests needed). |
| GH:042 | plain-centered | test 5 | Name "JOHN SMITH L. DOE", subtitle, location and contact row are all centred on the page. A heavy double rule appears directly above "WORK EXPERIENCE" — but the identical double-rule motif recurs above every subsequent section heading (Education, Leadership & Activities, Projects) throughout the document, indicating it is a per-heading decoration (an overline "belonging to" each section heading) rather than a header-specific divider → excluded under A2 ("whichever side of the heading they sit"). → plain-centered. |
| NPM:048 | ruled | rule test (3) | Name "Jane Developer" + blue subtitle top-left; a thin full-width rule sits directly below the subtitle (last header-block line, A1) and directly above the un-headed summary paragraph, before "EXPERIENCE" (whose own thick underline is a separate, excluded section-heading rule, A2). This is one of the two items the Addendum A1 text explicitly names as satisfying the amended ruled test. → ruled. |
| NPM:039 | plain-left | elimination (2→6) | Name "Richard Hendriks" + "Programmer" subtitle, left-aligned. A full-width rule follows immediately, before the "Personal Info" heading — but the identical rule style recurs before every subsequent section (Work Experience, Education, Volunteer Work, Publications, …), consistently as a heading overline throughout the document, so it reads as a per-heading decoration rather than a header-specific divider → excluded per A2, same reasoning as GH:042. No band, no split (contacts sit below in the Personal Info section, same side as name, not opposite). Name clearly left-flush, not centred → plain-left. |
| GH:036 | plain-left | elimination (2→6) | PDF rendered to PNG for viewing. "JOHN DOE" + subtitle top-left in the main (white) column; a light-gray sidebar tint runs the full page height in the right column (contact info, skills) — fill not behind the name at all → band (a) fails regardless of (d). No full-width rule beneath the header. Split fails: the sidebar's "CONTACT INFO" heading starts well more than one name-line-height below the name's baseline, so vertical overlap/proximity condition (c) is not met. Name is left-flush, clearly off-centre → plain-left. |
| GH:061 | split | split test (4) | LaTeX moderncv-style PDF rendered to PNG. "Mark Liu" large name flush to the left margin; a contact block (icons omitted-for-web placeholders, email, url) flush to the right margin, same header row, vertically overlapping the name's line → split (a)-(c) all satisfied. |
| GH:041 | plain-left | elimination (2→6); low-confidence — no visible name | This preview appears to open directly on an "About" section heading with no distinct, larger personal-name heading above it visible in the supplied image (the candidate's actual name is not shown in this crop). Treating the top-of-page heading as the effective header line for scoring purposes: it is left-aligned, on plain background, no fill or rule precedes it. Flagged low-confidence since a true name element may exist above this crop that was not captured by the preview URL. → plain-left. |
| NPM:021 | plain-left | elimination (2→6); (3a) sidebar-rule fails explicitly | Left column is a dark-blue full-page-height "COMPETENCES" sidebar — fails band (a)/(d) (not behind name; full height). A blue rule follows "Web Developer" but spans only the main-column width (≈59% of page width / ≈59% of live width, given live width runs page-wide once the sidebar's own text is counted) — explicitly the "sidebar-only or column-only rules fail (a)" case named in the definition → not ruled. No split (contact icons sit under the name, same side, not opposite the sidebar's skills list). Name centre measured ≈6% of page width from true page centre — just over the 5% threshold, recorded as borderline → plain-left. |

## Tally
plain-left: 11 (GH:051, NPM:040, GH:024, NPM:005, NPM:079, NPM:002, GH:036, GH:041, NPM:021, NPM:039, plus none double-counted — recount: GH:051, NPM:040, GH:024, NPM:005, NPM:079, NPM:002, GH:036, GH:041, NPM:021, NPM:039 = 10) — see table for exact per-id values; totals: plain-left 10, ruled 3 (NPM:068, GH:013, NPM:048), plain-centered 3 (GH:076, NPM:027, GH:042), split 2 (GH:055, GH:061), band 1 (GH:019), image-hero 1 (NPM:067).

Scratch images were downloaded to `research/designs-evidence/tmp-cv3/` for viewing and are deleted
at the end of this task per instructions.
